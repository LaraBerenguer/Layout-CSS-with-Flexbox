# Responsive Layout CSS and HTML with Flexbox

A simple layout made with flexbox. Responsive. 

# Installation

Download both files and save them on the same folder. This is an static layout so nothing else is needed.
